import { createMuiTheme } from '@material-ui/core/styles';
const theme = createMuiTheme({
    palette: {
       
      },
    color: {
         primary:'#2962ff',
         success:'#64dd17',
         erro: '#e64a19',
         textColor:'#ffff',
    }
});

export default theme;